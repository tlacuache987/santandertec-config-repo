spring:
  jpa:
    hibernate:
      ddl-auto: update
    #show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

  datasource:
    url: ${db.datasource.url}
    #url: jdbc:mysql://localhost:3306/vault-employee-dev
    username: ${db.username}
    password: ${db.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    
hello: Hello from Github and Spring Cloud Config Server (dev profile)
